Actividad 1
    1. Componentes: Se utiliza para dividir la interfaz en bloques independientes: `App.jsx`, `RecetaCard.jsx`, `ListaRecetas.jsx` y `FiltroCategoria.jsx`.
                    Es el adecuado para este contexto, debido a que permite la divición de tareas evitando codigos grandes y facilitando el mantenimientos de este mismo.
    2. JSX: Se emplea en el (`return`) de todos los componentes de la aplicación para definir la estructura visual.
            Es el adecuado, ya que permite escribir una sintaxis que combina HTML y JavaScript en un solo lugar, facilitando la inyección directa y segura de variables reactivas.
    3. props: 
              
    4. Estado (useState): 
    5. renderizado de listas (.map): 
    6. Renderizado condicional: 

Actividad 2
    1. 
    2. 

